// Example IDL file for our monster's schema.

namespace Edgar;

table HeaderValue {
    name: string (required);
    value: string;
}

table Message {
    headers: [HeaderValue];
    body: [byte];
}

root_type Message;
